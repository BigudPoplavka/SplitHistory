
ADD:

1. уменьшение размера точек на слое по мере зума + порог появления названия заметки около узла (как в обсидиан)
2. Красивые тонкие скролбары вместо стандартных windows
3. Фон канваса должен иметь фон из однородных точечек (сетка)
4. Панель с деревом заметок слева и панель со слоями справа должно быть можно делать шире / уже драг-курсором

CREATE:

1.ПРОЕКТ: 
- 4.1. Нужно разработать концеацию проекта:
-- Для проекта создается папка, заметки хранятся в md
-- Для хранения всего проекта, слоев, заметок, тегов,ссылок и др. сущностей нужно    
    выбрать формат файла и обосновать оптимальность по размеру
- 4.2. должно быть меню: файл -открыть и тд., все что понадобится
-- добавить возможность открывать папки как рабочие пространства (как в обсидиан)

ADAPT:

1. Использоание баз знаний из обсидиан (все заметки пока загружаются на отдельный слой Обсидиан):
1.1. выбрать все хранилище (с сохранением связи и ссылок)
1.2. выбрать любую подпапку (с сохранением связи и ссылок) 
1.3. Загруженные заметки пока должны сохранять только текст, разметку md, ссылки, теги, медиа - все самое важное внутри заметки.


